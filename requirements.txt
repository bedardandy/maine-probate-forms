# Core: routing, schema work, the fill plan/PDF path, and the MCP servers.
PyMuPDF>=1.24.0
pdfplumber>=0.11.0
Pillow>=10.2.0
openai>=1.40.0
pydantic>=2.0
click>=8.0
requests>=2.31.0
httpx>=0.27.0
numpy>=1.26.0
PyYAML>=6.0
mcp[cli]>=1.0.0

# Optional: PDF/UA accessibility remediation (tools/accessibility/).
# Needed only for make_accessible.py / remediate_form.py; the fill path runs
# without these.
pikepdf>=8.0
fonttools>=4.40

# Optional: the FastAPI HTTP surface (tools/api_server.py). Uncomment to run it.
# fastapi>=0.110
# uvicorn>=0.29
