# Core: routing, schema work, the fill plan/PDF path, and the MCP servers.
maine-forms-engine @ git+https://github.com/bedardandy/maine-forms-engine@v0.6.0
                   # shared drift guard (tools/check_upstream.py), PDF/UA
                   # accessibility remediation (tools/accessibility/ are shims
                   # over it), and the MCP scaffold tools/agent_server.py
                   # builds on; the geometry fill path stays local
# AGPL-3.0-or-commercial (Artifex) — see the licensing notice in README.md
PyMuPDF>=1.24.0
pdfplumber>=0.11.0
Pillow>=10.2.0
openai>=1.40.0
pydantic>=2.0
click>=8.0
requests>=2.31.0
httpx>=0.27.0
numpy>=1.26.0
PyYAML>=6.0
mcp[cli]>=1.0.0

# PDF/UA accessibility remediation deps (pulled in by maine-forms-engine;
# kept pinned here for clarity).
pikepdf>=8.0
fonttools>=4.40

# Optional: the FastAPI HTTP surface (tools/api_server.py). Uncomment to run it.
# fastapi>=0.110
# uvicorn>=0.29
